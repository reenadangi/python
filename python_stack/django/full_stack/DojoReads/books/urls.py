from django.urls import path

from . import views
from django.conf.urls import url

urlpatterns = [
    # path('home/', views.home, name='books_home'),
    # path('add', views.add, name='books_add'),
    
 
]